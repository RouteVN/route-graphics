$schema: http://json-schema.org/draft-07/schema#
title: Hover Event Return
description: This event type is called "hover" and will be invoked when the pointer is hover on an element.
type: object
properties:
  _event:
    type: object
    required:
      - id
    properties:
      id:
        type: string
        description: The ID of the element that was clicked
  payload:
    type: object
    description: Payload object that is passed through the hover object in the input