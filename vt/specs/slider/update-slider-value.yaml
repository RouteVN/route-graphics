---
title: Update Slider Value
description: Test updating slider initial value and split-track reveal
specs:
  - slider should update its thumb position when initialValue changes
  - slider should preserve the inactive track while resizing the active reveal
  - slider should recalculate the split-track mask correctly
steps:
  - action: keypress
    key: "n"
  - action: screenshot
  - action: keypress
    key: b
  - action: screenshot
---
states:
  - elements:
      - id: slider-update-test
        type: slider
        direction: horizontal
        thumbSrc: horizontal-idle-thumb
        barSrc: slider-split-active-bar
        inactiveBarSrc: slider-split-inactive-bar
        width: 200
        height: 20
        x: 50
        "y": 50
        alpha: 1
        anchorX: 0
        anchorY: 0
        zIndex: 1
        min: 0
        max: 100
        step: 1
        initialValue: 25
  - elements:
      - id: slider-update-test
        type: slider
        direction: horizontal
        thumbSrc: horizontal-idle-thumb
        barSrc: slider-split-active-bar
        inactiveBarSrc: slider-split-inactive-bar
        width: 200
        height: 20
        x: 50
        "y": 50
        alpha: 1
        anchorX: 0
        anchorY: 0
        zIndex: 1
        min: 0
        max: 100
        step: 1
        initialValue: 75
