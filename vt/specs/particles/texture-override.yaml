---
title: Particles with texture override
steps:
  - customEvent snapShotKeyFrame deltaMS=300
  - screenshot
  - customEvent snapShotKeyFrame deltaMS=300
  - screenshot
  - keypress n
  - customEvent snapShotKeyFrame deltaMS=300
  - screenshot
  - customEvent snapShotKeyFrame deltaMS=300
  - screenshot
---
states:
  # Use snow preset but with circle texture instead of snowflake
  - elements:
      - id: 'snow-circles'
        type: "particles"
        preset: "snow"
        texture: "circle"
        count: 150
        emitter:
          seed: 12345

  # Use rain preset but with snowflake texture
  - elements:
      - id: 'rain-flakes'
        type: "particles"
        preset: "rain"
        texture: "snowflake"
        count: 200
        emitter:
          seed: 12345
