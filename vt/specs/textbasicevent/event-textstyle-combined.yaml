---
title: Text Style Change on Hover AND Click
description: Test textStyle in both hover and click events working together
specs:
  - "hover textStyle should work"
  - "click textStyle should work"
  - "both events can have different textStyles"
steps:
  - move 220 160
  - screenshot
  - holdLeftDown
  - screenshot
  - releaseLeftUp # We want to check when the pointer is realease inside the element and outside of it too
  - screenshot
  - holdLeftDown
  - screenshot
  - move 0 0
  - screenshot
  - releaseLeftUp
  - screenshot
  - move 220 310
  - screenshot
  - holdLeftDown
  - releaseLeftUp # We want to check when the pointer is realease inside the element and outside of it too
  - screenshot
  - holdLeftDown
  - screenshot
  - move 0 0
  - screenshot
  - releaseLeftUp
  - screenshot
---
states:
  - elements:
      - id: 'text-both-1'
        type: "text"
        x: 200
        y: 150
        content: "Hover (red) then Click (orange)"
        textStyle:
          fontFamily: "Arial"
          fontSize: 42
          fill: "#3498db"
        hover:
          cursor: "pointer"
          textStyle:
            fill: "#e74c3c"
            fontSize: 42
            fontFamily: "Arial"
        click:
          textStyle:
            fill: "#f39c12"
            fontSize: 48
            fontFamily: "Arial"
      - id: 'text-both-2'
        type: "text"
        x: 200
        y: 300
        content: "Hover grows, Click changes font"
        textStyle:
          fontFamily: "Arial"
          fontSize: 36
          fill: "#2ecc71"
        hover:
          cursor: "pointer"
          textStyle:
            fill: "#2ecc71"
            fontSize: 52
            fontFamily: "Arial"
        click:
          textStyle:
            fill: "#9b59b6"
            fontSize: 48
            fontFamily: "Georgia"