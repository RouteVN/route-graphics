---
title: Text Style Change on Hover AND Click
description: Test textStyle in both hover and click events working together; color change is intentional to validate textStyle.fill behavior (guideline exception).
specs:
  - "hover textStyle should work"
  - "click textStyle should work"
  - "both events can have different textStyles"
steps:
  - move 220 160
  - screenshot
  - mouseDown
  - screenshot
  - mouseUp # We want to check when the pointer is realease inside the element and outside of it too
  - screenshot
  - mouseDown
  - screenshot
  - move 0 0
  - screenshot
  - mouseUp
  - screenshot
  - move 220 310
  - screenshot
  - mouseDown
  - mouseUp # We want to check when the pointer is realease inside the element and outside of it too
  - screenshot
  - mouseDown
  - screenshot
  - move 0 0
  - screenshot
  - mouseUp
  - screenshot
---
states:
  - elements:
      - id: 'text-both-1'
        type: "text"
        x: 200
        y: 150
        content: "Hover (red) then Click (orange)"
        textStyle:
          fontFamily: "Arial"
          fontSize: 42
          fill: "#A6A6A6"
        hover:
          cursor: "pointer"
          textStyle:
            fill: "#FFFFFF"
            fontSize: 42
            fontFamily: "Arial"
        click:
          textStyle:
            fill: "#D9D9D9"
            fontSize: 48
            fontFamily: "Arial"
      - id: 'text-both-2'
        type: "text"
        x: 200
        y: 300
        content: "Hover grows, Click changes font"
        textStyle:
          fontFamily: "Arial"
          fontSize: 36
          fill: "#737373"
        hover:
          cursor: "pointer"
          textStyle:
            fill: "#737373"
            fontSize: 52
            fontFamily: "Arial"
        click:
          textStyle:
            fill: "#4D4D4D"
            fontSize: 48
            fontFamily: "Georgia"
