---
title: Slider Value Template Replacement
description: Test payload return
specs:
  - "change event should return the current value of the thumb and the action payload"
  - "template should work for both min and max values"
  - "template should work for intermediate values"
  - "actionPayload should contain properly formatted value"
steps:
  - move 120 160
  - mouseDown
  - screenshot
  - move 180 160
  - screenshot
---
states:
  - elements:
      - id: 'slider-template-test'
        type: "slider"
        direction: "horizontal"
        thumbSrc: "horizontal-idle-thumb"
        barSrc: "horizontal-idle-bar"
        width: 200
        height: 20
        x: 100
        y: 150
        alpha: 1
        anchorX: 0
        anchorY: 0
        zIndex: 1
        min: 10
        max: 50
        step: 2
        initialValue: 30
        change:
          actionPayload:
            action: "volume-change"
            sliderId: "main-volume"
            status: "adjusting"
            range: "10-50"